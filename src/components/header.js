import React from 'react';

import { HeaderBar } from './styledComponent';
import useStyles from '../style/style';

const Header = () => {
  const classes = useStyles();

  return (
    <HeaderBar />
  )
}

export default Header
